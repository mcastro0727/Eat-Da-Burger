  
<li>

	{{#unless devoured}}
	<button class="change-devour btn btn-outline-info" data-id="{{id}}" data-newdevour="{{devoured}}">
		Devour It!! <i class="fas fa-utensils"></i>
	</button>
	{{/unless}}

	{{burger_name}}
</li>